(function(){'use strict';document.addEventListener('DOMContentLoaded',function(){const input=document.getElementById('category-search'),sort=document.getElementById('category-sort'),grid=document.getElementById('category-tools'),empty=document.getElementById('category-empty');if(!grid)return;const cards=[...grid.querySelectorAll('.category-tool-card')];function render(){const q=(input?.value||'').trim().toLowerCase(),mode=sort?.value||'name';let visible=cards.filter(c=>`${c.dataset.toolName||''} ${c.dataset.toolDescription||''}`.includes(q));visible.sort((a,b)=>{const x=a.dataset.toolName||'',y=b.dataset.toolName||'';return mode==='name-desc'?y.localeCompare(x):x.localeCompare(y)});cards.forEach(c=>c.hidden=true);visible.forEach(c=>{c.hidden=false;grid.appendChild(c)});if(empty)empty.hidden=visible.length!==0}input?.addEventListener('input',render);sort?.addEventListener('change',render);render()})})();
